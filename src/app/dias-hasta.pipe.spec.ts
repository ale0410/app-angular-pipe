import { DiasHastaPipe } from './dias-hasta.pipe';

describe('DiasHastaPipe', () => {
  it('create an instance', () => {
    const pipe = new DiasHastaPipe();
    expect(pipe).toBeTruthy();
  });
});
